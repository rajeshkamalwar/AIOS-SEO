---
id: "GAP-029"
title: "Vault guard snapshot omitted new untracked reports"
status: "CLOSED"
severity: "MEDIUM"
scope: "Disposable validator test snapshot; no application behavior or data authority"
owner_role: "Engineering"
specs: ["vault/00-HOME.md"]
implementation: ["vault/test_refresh.py"]
tests: ["vault/test_refresh.py"]
reports: ["reports/POST-VAULT-READONLY-CORRECTIONS.md"]
closure_evidence: "reports/POST-VAULT-READONLY-CORRECTIONS.md"
closure_required: "Copy newly authored project artifacts before staging, exclude unrelated user files, and pass conformance guards against the resulting checkpoint."
---

# GAP-029 — incomplete disposable snapshot

## History

- OPEN — final checkpoint guard setUpClass failed after creation of an untracked implementation report: git-ls-files-only copy omitted the referenced report.
- FIX_IN_PROGRESS — reproduce snapshot omission; retain normal source/link validation rather than bypassing it.
- FIXED — include nonignored new project artifacts in the isolated copy; unrelated root user files remain excluded. Added a regression for both conditions; full guard rerun pending.

- TESTED — final10guard tests pass, including newly untracked report inclusion and unrelated-file exclusion.
- VERIFIED — isolated snapshot behavior reviewed against the failure; production/application files are not mutated by guard tests.
- CLOSED — disposable test snapshot defect only.
